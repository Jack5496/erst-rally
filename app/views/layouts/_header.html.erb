<% if logged_in? %>
<% if current_user.admin? %>
    <header class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">
        <%= link_to current_user.name, current_user, id: "logo" %>
        <nav>
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to "Hilfe", help_path %></li>
            <li><%= link_to "Beamer", beamer_url %></li>
            <li><%= link_to "Game", game_url %></li>
            <li><%= link_to "Rallys", rallies_url %></li>
            <li><%= link_to "Stationen", stations_path %></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Station <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", current_user %></li>
                <li><%= link_to "Einstellungen", edit_station_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Abmelden", logout_path, method: "delete" %>
                </li>
              </ul>
            </li>
          </ul>
        </nav>
      </div>
    </header>
<% end %>
<% end %>
